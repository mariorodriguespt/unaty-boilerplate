/*
    We're declaring a global variable that can be accessed on the client and server
    Every package declares a context and links constants, configurations, functions, etc.

    By default, Meteor users underscore library so you can use:

        _.extend( UN , {
            Context : {
                Config      : {}
                Schemas     : {}
                Collections : {}
            }
        } );
*/

UN = {

};